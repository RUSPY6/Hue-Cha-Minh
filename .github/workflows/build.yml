name: Build Character
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu latest
    steps:
      - uses: actions/checkout@v2
      - name: Zip
        run: |
          mkdir DootyEnjoyer6-Hue_Cha_Minh
          cd
